"""SLM phát hiện parse sai và đề xuất parse mới.

Module ưu tiên recall, nhận bản parse hiện tại và trả nghi vấn cùng một parse
mới. Parse mới chỉ là giả thuyết cạnh tranh, không phải đáp án cuối cùng và
không được ghi đè trace của lớp trước.

Chưa implement. Xem BUILD.md §7 để biết thứ tự triển khai.
"""
